package org.javautil.reflect;

import java.util.HashMap;
import java.util.Map;

public class FieldHelperTest {

	@SuppressWarnings("serial")
	Map<String, String> values = new HashMap<String, String>();

	/*
	 * @Test public void testClass1() { values.clear(); values.put("stringField",
	 * "A"); values.put("longNumberField", "32");
	 * 
	 * Class1 cl = new Class1(); FieldHelper fh = new FieldHelper(); fh.set(cl,
	 * values); assertEquals("A", cl.stringField); assertEquals(new Long(32),
	 * cl.longNumberField); }
	 * 
	 * @Test public void test2() { values.clear(); values.put("privateIntegerField",
	 * "64");
	 * 
	 * Class1 cl = new Class1(); FieldHelper fh = new FieldHelper(); fh.set(cl,
	 * values); assertEquals("A", cl.stringField); assertEquals(new Long(64),
	 * cl.longNumberField); assertNull(cl.stringField);
	 * 
	 * }
	 */
}
